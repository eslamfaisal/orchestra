# orch

Compiling M0-01 package shell. Runtime behavior is intentionally deferred to its planned implementation step.

Run `pnpm --filter orch build`, `lint`, `typecheck`, or `test` from the repository root.
