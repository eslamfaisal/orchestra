# @orchestra/provider-opencode

Compiling M0-01 package shell. Runtime behavior is intentionally deferred to its planned implementation step.

Run `pnpm --filter @orchestra/provider-opencode build`, `lint`, `typecheck`, or `test` from the repository root.
