---
description: Show where we are in the plan and what can start now.
---
Delegate to `orchestra-scout`: from `plan/PROGRESS.md` list the current milestone, steps by status (✅/🧪/🟨/⛔ counts), and every ⬜ step whose "Depends on" steps are all ✅ (read the dependency column in `plan/ROADMAP.md`). For each startable step include its tier/agent from `plan/AGENT_ROUTING.md`. Present it as a short table and recommend the next `/step` to run (dependency order, MVP first).
